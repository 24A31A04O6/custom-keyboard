package com.babeltech.babelkey.core.input

/**
 * GestureScorer — Phase 2 stub for Viterbi-style glide scoring.
 *
 * Phase 1: returns empty; Phase 2: scores path against dictionary words.
 */
object GestureScorer {
    fun score(path: GesturePath, candidates: List<String>): List<String> = emptyList()
}
