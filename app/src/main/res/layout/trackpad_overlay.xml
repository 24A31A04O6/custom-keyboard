<?xml version="1.0" encoding="utf-8"?>
<!--
    Trackpad mode overlay.
    Shown over the keyboard when spacebar is long-pressed.
    User slides finger to move cursor.
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/trackpad_overlay"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:background="@drawable/trackpad_bg"
    android:padding="16dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="👆 Trackpad Mode"
        android:textColor="#FFFFFF"
        android:textSize="16sp"
        android:textStyle="bold"
        android:paddingBottom="8dp"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Slide to move cursor • Release to exit"
        android:textColor="#AAAAAA"
        android:textSize="12sp"/>

</LinearLayout>